---
description: Keep one ASP.NET UI stack (Razor Pages or MVC)
globs: ["**/*.{cshtml,razor}", "**/Pages/**", "**/Views/**", "**/Controllers/**"]
alwaysApply: false
---

# One stack (ASP.NET UI)

- Keep the stack the repo already uses: Razor Pages (`Pages/` with `@page` on line 1) or MVC (`Views/` + Controllers).
- A feature uses one UI framework; a second one needs the user's request.
