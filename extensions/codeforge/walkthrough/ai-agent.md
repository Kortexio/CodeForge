# AI Agent

The Agent panel sits in the right sidebar (**AI**).

- **Ask** — questions without tools  
- **Agent** — plan, edit, and verify with approvals  
- **Auto** — allow edits + shell for the session  

Tip: type `/` in the composer for slash commands like `/instructions`.
