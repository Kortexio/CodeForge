# Acme.Router

Tiny HTTP route table in .NET 10, built card by card (`docs/C01.md` … `docs/C05.md`).

```
Acme.Router.slnx
src/Acme.Router/          — library (namespace Acme.Router)
tests/Acme.Router.Tests/  — xUnit
docs/C01.md … C05.md      — cards with acceptance criteria
```

```bash
dotnet test Acme.Router.slnx
```
